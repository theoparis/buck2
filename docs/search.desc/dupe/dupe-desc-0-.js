searchState.loadedDescShard("dupe", 0, "A cheap version of <code>Clone</code>.\nDerive the <code>Clone</code> trait, but without requiring all type …\nDerive the <code>Copy</code> trait, but without requiring all type …\nLike <code>Clone</code>, but should only be available if <code>Clone</code> is …\nDerive the <code>Dupe</code> trait.\nDerive the <code>Dupe</code> trait, but without requiring all type …\nExtension traits on <code>Option</code> where it holds a ref.\nLike <code>duped()</code>, but only works for types that implement <code>Dupe</code>.\nLike <code>cloned</code>, but with a <code>Dupe</code> constraint.")